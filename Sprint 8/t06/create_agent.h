#ifndef TAPOK
#define TAPOK
#include <stdio.h>
#include <stdlib.h>
#include "agent.h"

char *mx_strnew(const int );

char* mx_strcpy(char* dst, const char* );

int mx_strlen(const char *);
int mx_strlen(const char *);
char *mx_strnew(const int );
#endif
